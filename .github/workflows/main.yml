name: Job Isolation Test

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Create file
        run: echo "Hello from build job" > test.txt

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Read file
        run: cat test.txt
